# Streamlit Cloud Requirements (Minimal for Cloud Deployment)
streamlit>=1.28.0
pandas>=1.5.0
numpy>=1.24.0
plotly>=5.15.0
requests>=2.31.0

# Try to install RDKit and chemistry packages, but don't fail if they're not available
rdkit-pypi>=2022.9.5; sys_platform != "linux"
rdkit>=2022.9.5; sys_platform == "linux"

# 3D visualization (optional)
stmol>=0.0.9
py3Dmol>=2.0.4
ipython_genutils>=0.2.0
